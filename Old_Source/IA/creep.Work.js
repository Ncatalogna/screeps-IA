/*
 * Module code goes here. Use 'module.exports' to export things:
 * module.exports.thing = 'a thing';
 *
 * You can import it from another modules like this:
 * var mod = require('creep.Work');
 * mod.thing == 'a thing'; // true
 */
//
//Using
var _const = require('Constants');
var rol = require('creep.Rol');
//
module.exports = {
    	newClass: function(dbIn){
	    this.db = dbIn;
	},
	RealizarWork: function(creep){ RealizarWork(creep); };
};

function Asignar(creep){
    var works = rol.
}

function RealizarWork(creep){
    
    switch(creep.memory.work){
        
        case _const.:
            Standby(creep);
            break;
            
        case "controlle-up":
        
            break;
        
        case "transfer":
            
            break;
        
        case "recharge":
            
            break;
            
        case "build":
            
            break;  
            
        case "retirada":
            
            break;
            
    }
    
}

function Standby(creep)
{
    
}
